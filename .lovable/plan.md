# Restore the preview

## Plan
1. Reconnect the preview to the running app after its recent restart.
2. Confirm the home page renders in a fresh browser session.
3. If the page remains blank, inspect the rendered page and network requests, then fix only the confirmed failure.

## Verified current state
- The app server is running and returns the Chiropractic Patient Report Generator page successfully.
- No current browser runtime error is recorded.
- The server recently restarted after an environment change.
